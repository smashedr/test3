---
kind: pipeline
name: test-aarch64

platform:
  arch: aarch64

trigger:
  event:
    - push

steps:
- name: test
  image: docker:dind
  volumes:
  - name: dockersock
    path: /var/run/docker.sock
  commands:
  - docker info

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock
